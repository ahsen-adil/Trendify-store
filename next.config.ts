import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['img.drz.lazcdn.com','media.istockphoto.com', 'another-domain.com','img.lazcdn.com','i.imgur.com'], 
  },
};

export default nextConfig;
